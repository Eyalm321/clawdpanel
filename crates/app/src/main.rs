//! ClawdPanel (Rust/Slint rewrite) -- app entry point.
//!
//! S1 (#48) in progress: the workspace builds. The frameless, always-on-top
//! Slint bar window lands in the next increment.

fn main() {
    println!("clawdpanel (rust/slint rewrite): scaffold build OK");
}
